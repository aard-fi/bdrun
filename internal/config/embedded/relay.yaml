# Embedded relay configuration (placeholder)
dns:
  enabled: false

websocket:
  enabled: false

auth:
  type: token
  token: "change-me"

limits:
  max_connections: 1000
  max_message_size: 10485760
  connection_timeout: 30s
  idle_timeout: 5m

logging:
  level: info
  format: json
  output: stdout
